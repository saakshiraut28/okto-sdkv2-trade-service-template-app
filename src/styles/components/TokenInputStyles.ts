const styles: { [key: string]: React.CSSProperties } = {
  tokenActions: {
    marginTop: "8px",
  },

  nativeButton: {
    padding: "4px 8px",
    backgroundColor: "#333",
    color: "#fff",
    border: "1px solid #555",
    borderRadius: "4px",
    fontSize: "12px",
    cursor: "pointer",
  },
};

export default styles;
